# Django app configuration
from django.apps import AppConfig
# AppliancesConfig section
class AppliancesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'appliances'
